import ContactSection from "../components/ContactSection/ContactSection";

/**
 * Purpose:
 * Hosts the standalone contact route for visitors who arrive directly at the
 * "Contacto" page and need the full "Hablemos" experience.
 */
export default function Contact() {
  return <ContactSection />;
}
