import ProductsPageContent from "../components/ProductsPageContent/ProductsPageContent";

/**
 * Purpose:
 * Hosts the full catalog route behind the "PRODUCTOS" navigation entry and
 * gives buyers a dedicated place to browse the Soltex offer.
 */
export default function Products() {
  return <ProductsPageContent />;
}
